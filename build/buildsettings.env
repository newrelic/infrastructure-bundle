#!/usr/bin/env sh
# This file is loaded by GH to set env vars in the CI environment
# This is done to avoid inconsistencies between settings used in build and release workflows
DOCKER_PLATFORMS=linux/amd64,linux/arm64,linux/arm
AGENT_VERSION=latest
JRE_VERSION=
DOCKER_IMAGE=newrelic/infrastructure-bundle
